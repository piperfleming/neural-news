-- Database initialisation — runs once on first container start.
-- Tables are created automatically by SQLAlchemy on backend startup,
-- so this file is only for extensions or roles you need ahead of time.

CREATE EXTENSION IF NOT EXISTS "uuid-ossp";
